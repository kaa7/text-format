#ifndef OREDERED_LIST_H
#define OREDERED_LIST_H
#include "utils.h"

void ordered_list(char list_type, char special_character, char ordering,
                  int start_line, int end_line,
                  char result[MAX_LINES][MAX_CHARS_PER_LINE]);

#endif
